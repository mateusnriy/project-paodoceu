# Variáveis de Ambiente - Pão do Céu

# Configurações do Servidor
NODE_ENV=development
PORT=3333
LOG_LEVEL=info # Níveis: error, warn, info, http, verbose, debug, silly

# URL de Conexão com o Banco de Dados (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Chaves Secretas (USE VALORES SEGUROS GERADOS ALEATORIAMENTE - min 32 caracteres)
# Use no console Node: require('crypto').randomBytes(32).toString('hex')
JWT_SECRET="GERAR_UMA_CHAVE_SEGURA_AQUI_min_32_chars"
CSRF_SECRET="GERAR_UMA_CHAVE_SEGURA_AQUI_min_32_chars"
COOKIE_SECRET="GERAR_UMA_CHAVE_SEGURA_AQUI_min_32_chars"

# Configuração do Frontend 
FRONTEND_ORIGIN="http://localhost:5173" # Endereço do frontend em dev (sem /api no final)